/*=============================Additionally this java script is used as text switcher between the text's i listed below.====================*/

var typed = new Typed(".typing", {
     strings:["A CompSci Student", "Studying in Partido State University"],
     typeSpeed:80,
     backSpeed:80,
     loop:true
})